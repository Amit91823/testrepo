#include<stdio>
#include<conio.h>

	int main(){
	int n,i,j,temp;
	int array[5]={3,4,7,8,9};
		for(i=0;i<=n;i++){
			for(j=1;j<=n-1;j++)
				{
				temp = arr[j];
				arr[j] = arr[j-1];
				arr[j-1] = temp;
				j--;
				}
				printf("Sorted list in assanding orderd:\n",arr(i));
			}
			
			
	}
